{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "KAFI AI-DLC v0.7 — team settings (committed to repo)",
  "skills": {
    "_comment": "Manual skills loaded only when explicitly requested by user",
    "inclusion": "manual"
  },
  "permissions": {
    "_comment": "Restrict to project files + git; deny network reads of credentials"
  }
}
